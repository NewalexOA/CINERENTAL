# Исправление проблемы восстановления бэкапов на разных компьютерах

## Проблемы

### 1. Восстановление бэкапов

При попытке восстановления бэкапа, созданного на одном компьютере, на другом компьютере возникала ошибка:

```
Ошибка восстановления: /Users/anaskin/Documents/ACT-Rental-Backups/20260112_155901/restore_backup.sh: line 24: cd: /Users/actrental/Documents/GitHub/CINERENTAL: No such file or directory
```

Причина: скрипт восстановления содержал жёстко заданный путь к проекту с компьютера, где был создан бэкап.

### 2. Запуск приложения через лаунчер

После исправления бэкапов, приложение не могло запустить проект:

```
Не удалось запустить ACT-Rental: Файл docker-compose.prod.yml не найден. Проверьте путь к проекту.
```

Причина: `docker_manager.py` содержал хардкоженный путь `~/Documents/GitHub/CINERENTAL`.

## Решение

### 1. Исправлен скрипт создания бэкапов

Обновлён файл [scripts/create_backup.sh](../scripts/create_backup.sh) - теперь генерируемый скрипт `restore_backup.sh` автоматически ищет проект ACT-Rental:

- Проверяет текущую директорию и родительские директории
- Ищет в стандартных местах (~/Github/CINERENTAL, ~/Documents/GitHub/CINERENTAL, и т.д.)
- Если не найдёт автоматически, запросит путь у пользователя

### 2. Исправлен Python код лаунчера

**backup_utils.py:** Обновлён файл [src/backup/backup_utils.py](src/backup/backup_utils.py) - удалён hardcoded путь `/Users/actrental/Documents/GitHub/CINERENTAL`.

**docker_manager.py:** Обновлён файл [src/docker_manager.py](src/docker_manager.py) - теперь использует функцию `get_project_root()` для автопоиска проекта:

```python
# Было:
self.project_path = project_path or os.path.expanduser('~/Documents/GitHub/CINERENTAL')

# Стало:
if project_path:
    self.project_path = project_path
else:
    from backup.backup_utils import get_project_root
    try:
        self.project_path = get_project_root()
    except FileNotFoundError:
        self.project_path = None
```

### 3. Исправлен существующий бэкап

Обновлён скрипт восстановления в уже созданном бэкапе `/Users/anaskin/Documents/ACT-Rental-Backups/20260112_155901/restore_backup.sh`.

## Как использовать

### Восстановление из бэкапа

1. Перейдите в директорию бэкапа:
   ```bash
   cd ~/Documents/ACT-Rental-Backups/20260112_155901
   ```

2. Запустите скрипт восстановления:
   ```bash
   ./restore_backup.sh
   ```

3. Скрипт автоматически найдёт проект или попросит указать путь вручную

### Создание нового бэкапа

Все новые бэкапы, созданные после этого исправления, будут автоматически использовать универсальный скрипт восстановления.

## Технические детали

Скрипт восстановления теперь использует функцию `find_project_root()`, которая:

1. **Метод 1**: Поиск от текущей директории вверх (до 5 уровней)
   - Ищет файлы `docker-compose.yml` или `docker-compose.prod.yml`

2. **Метод 2**: Проверка стандартных путей:
   - `$HOME/Documents/GitHub/CINERENTAL`
   - `$HOME/Github/CINERENTAL`
   - `$HOME/github/CINERENTAL`
   - `$HOME/Projects/CINERENTAL`
   - `$HOME/Documents/Projects/CINERENTAL`

3. **Метод 3**: Если ничего не найдено - запрос у пользователя

## Тестирование

Все исправления протестированы и работают:

### Скрипт восстановления бэкапов:
- ✅ Синтаксис проверен
- ✅ Автопоиск директории работает
- ✅ Находит проект: `/Users/anaskin/Github/CINERENTAL`
- ✅ Совместим с Docker Compose

### Лаунчер приложения:
- ✅ Приложение пересобрано (v2, 12 января 2026)
- ✅ Автопоиск проекта через `get_project_root()` работает
- ✅ DockerManager корректно находит `docker-compose.prod.yml`
- ✅ Защита от отсутствия пути к проекту

## Файлы

- 📄 Обновлённое приложение: `ACT-Rental-Launcher/src/dist/ACT-Rental Launcher.app`
- 📄 Исправленный бэкап: `~/Documents/ACT-Rental-Backups/20260112_155901/restore_backup.sh`
- 📝 Этот документ: `ACT-Rental-Launcher/BACKUP_RESTORE_FIX.md`

## Дата исправления

12 января 2026 (обе версии)
